# Test-only dependencies. The application itself needs nothing in this file —
# it is deliberately separate so the deployed image stays lean. Pinned for the
# same reproducibility reason as requirements.txt.
-r requirements.txt
pytest==9.1.1
